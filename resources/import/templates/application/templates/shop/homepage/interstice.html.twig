{% set imageInterstice = shop_image('imageInterstice') %}
{% if imageInterstice %}
    <div class="container mb-6 text-center">
        <img src="{{ imageInterstice }}" alt="" class="img-fluid" role="presentation">
    </div>
{% endif %}
